package constants

const (
	// The location in the Gin context where the auth token user id is stored
	AuthTokenUserIdContextKey = "authTokenUserId"
	// The location in the Gin context where the auth token claims are stored
	AuthTokenClaimsContextKey = "authTokenClaims"
)
