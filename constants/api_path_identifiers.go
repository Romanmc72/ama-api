package constants

// Params for path parameter identifiers in API request paths.
const (
	// The parameter at the end of the path when it is an identifier.
	QuestionIdPathIdentifier = "questionId"
	// The parameter on the path that points to a user id, but is not the root identifier for the path.
	UserIdPathIdentifier = "userId"
	// The parameter on the path that points to a list id, but is not the root identifier for the path.
	ListIdPathIdentifier = "listId"
)
